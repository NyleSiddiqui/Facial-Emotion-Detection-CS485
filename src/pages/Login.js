import React from "react";

import { withRouter } from "react-router-dom";

function Login({ history }) {
  return (
    <>
      <h1>Facial Emotion Detection Login</h1>
    </>
  );
}

export default withRouter(Login);
